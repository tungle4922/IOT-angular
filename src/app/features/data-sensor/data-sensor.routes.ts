import { Routes } from '@angular/router';
import { DataSensorComponent } from './data-sensor.component';

export const DATA_SENSOR_ROUTES: Routes = [
  { path: '', component: DataSensorComponent },
];
