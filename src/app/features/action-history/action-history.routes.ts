import { Routes } from '@angular/router';
import { ActionHistoryComponent } from './action-history.component';

export const ACTION_HISTORY_ROUTES: Routes = [
  { path: '', component: ActionHistoryComponent },
];
